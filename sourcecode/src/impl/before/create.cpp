// s[charToIndex[curChar]] = SELF;
// if (expression.length() > 2) {
//   char nextChar = expression[2];
//   if (expression.length() > 3 && expression[3] == '*') {
//     s[charToIndex[nextChar]] = create(expression.substr(2));
//   } else {
//     s[charToIndex[nextChar]] = create(expression.substr(3));
//   }
// } else {
//   // do nothing
// }